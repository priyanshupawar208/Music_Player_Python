🎵 Music Player – Windows Desktop App (PyQt5)

This project is a simple and modern music player built using Python (PyQt5). It allows users to upload, play, pause, stop, and navigate through audio tracks with a clean UI and smooth controls. The player supports popular audio formats such as .mp3, .wav, and .ogg.

🚀 Features

Play / Pause / Stop audio playback

Forward & Backward seek (5-second steps)

Volume control with mute/unmute

Interactive slider to track & control playback position

Live timestamp updates (current time + duration)

Dynamic title display of the currently playing track

Custom icons & styled UI

Error-handled, stable, and user-friendly design

🛠 Tech Stack

Python 3.x

PyQt5 (UI + Multimedia)

Custom UI widgets (VLayout, HLayout, IconButton, TextMessage)

QMediaPlayer for handling audio playback

📂 How It Works

Once launched, the player initializes the UI and waits for the user to upload a music file. After selecting a track, the player automatically begins playback while updating the UI (timestamps, slider, icons). Users can control playback using the footer buttons.
